/**
 * Provide some ability to render a simplified canvas representing the whole map.
 * The use would be able to click on this, and the corresponding cell position would be sent to something else (probably to re-center the main view onto this spot)
 */
export class Minimap {

}